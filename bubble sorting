#include<stdio.h>
void main()
{int a[10],i,j;
printf("Enter 10 numbers ");
scanf("%d",&a[i]);
for(i=0;i<10;i++)
{
  for( j=0;j<10-1;j++)
  { 
    if(a[j]>a[j+1])
    {
    t=a[j];
    a[j]=a[j+1];
    x[j+1]=t;
    }
   }
 }
 printf("After sorting");
 for(i=0;i<10;i++)
 prifn("%d",a[i]);
 }
